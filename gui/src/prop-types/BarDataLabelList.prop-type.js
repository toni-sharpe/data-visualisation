import PropTypes from 'prop-types'

const BarDataLabelListPropType = PropTypes.shape({
  'mean': PropTypes.string,
  'range': PropTypes.string,
  'stddev': PropTypes.string,
})

export default BarDataLabelListPropType
